{
  "name": "supernova-urdu-narrator",
  "version": "1.0.0",
  "private": true,
  "homepage": ".",
  "scripts": {
    "build": "mkdir -p dist && cp index.html index.tsx metadata.json App.tsx types.ts dist/ && cp -r components services dist/"
  },
  "engines": {
    "node": "20.x"
  }
}