
{
  "name": "supernova-urdu-narrator",
  "version": "1.0.0",
  "private": true,
  "homepage": ".",
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "@google/genai": "^1.34.0"
  },
  "scripts": {
    "start": "servor . index.html 8080 --reload",
    "build": "echo 'Static build complete' && mkdir -p dist && cp index.html index.tsx metadata.json App.tsx types.ts dist/ && cp -r components services dist/"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}